import { Doctor } from './../src/doctor.js';

describe('Doctor', () =>{
  test("Should correctly allow user to search for a doctor", ()=>{
    let doctor = new Doctor(dr stephens);
    expect(doctor).toEqual(dr stephens);
  });

});
